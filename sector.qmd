---
title: "SECTOR ANALYTICS"
page-layout: custom
format:
  html:
    toc: false
    css: styles.css
---

```{=html}
<script src="[https://cdn.tailwindcss.com](https://cdn.tailwindcss.com)"></script>
<script src="[https://cdn.jsdelivr.net/npm/chart.js](https://cdn.jsdelivr.net/npm/chart.js)"></script>
<link href="[https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css](https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css)" rel="stylesheet">

<style>
    /* --- ESTILOS DARK MODE (FNEE STYLE) --- */
    body { 
        background-color: #0f172a !important; 
        color: #e2e8f0; 
        font-family: 'Segoe UI', system-ui, sans-serif; 
        overflow: hidden; /* Sin scroll global */
        height: 100vh; 
        margin: 0; 
    }
    
    /* Layout Principal */
    .app-container {
        display: flex; flex-direction: column;
        height: 100vh; width: 100%;
        max-width: 1800px; margin: 0 auto;
        padding: 0 20px;
    }

    /* Pestañas */
    .tab-active { background-color: #1e293b; color: #2dd4bf; border: 1px solid #2dd4bf; }
    .tab-inactive { background-color: #0f172a; color: #94a3b8; border: 1px solid #334155; }
    .tab-inactive:hover { background-color: #1e293b; color: #fff; }
    
    /* Tarjetas */
    .card-dark { 
        background-color: #1e293b; 
        border: 1px solid #334155; 
        border-radius: 0.75rem; 
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.5); 
    }
    
    /* Tabla */
    thead th { 
        background-color: #0f172a; color: #94a3b8; 
        position: sticky; top: 0; z-index: 10;
        border-bottom: 1px solid #2dd4bf;
        padding: 12px; text-align: left;
    }
    tbody tr:hover { background-color: rgba(45, 212, 191, 0.05); }
    td { border-bottom: 1px solid #334155; color: #cbd5e1; padding: 10px 12px; }
    
    /* Scrollbar */
    ::-webkit-scrollbar { width: 6px; height: 6px; }
    ::-webkit-scrollbar-track { background: #0f172a; }
    ::-webkit-scrollbar-thumb { background: #334155; border-radius: 3px; }
    ::-webkit-scrollbar-thumb:hover { background: #475569; }

    /* Inputs */
    input { background-color: #0f172a !important; border-color: #334155 !important; color: #fff !important; }
    input:focus { border-color: #2dd4bf !important; outline: none; }

    .fade-in { animation: fadeIn 0.3s ease-in; }
    @keyframes fadeIn { from { opacity: 0; transform: translateY(5px); } to { opacity: 1; transform: translateY(0); } }
</style>

<div class="app-container">

    <nav class="flex-none bg-slate-950 text-white border-b border-slate-800 shadow-md z-50 mt-4 mb-6 rounded-xl">
        <div class="px-6 h-16 flex items-center justify-between">
            <div class="flex items-center gap-3">
                <div class="bg-emerald-500/10 p-2 rounded text-emerald-400 border border-emerald-500/20"><i class="fa-solid fa-bolt"></i></div>
                <div>
                    <h1 class="font-bold text-lg leading-tight text-white">FNEE Intelligence</h1>
                    <p class="text-[10px] text-emerald-400 uppercase tracking-wider font-bold">Strategic Dashboard</p>
                </div>
            </div>
            <div class="flex space-x-2">
                <button onclick="switchTab('dashboard')" id="btn-dashboard" class="tab-active px-4 py-2 rounded-lg text-sm font-medium transition-all flex items-center gap-2">
                    <i class="fa-solid fa-chart-column"></i> Mercado
                </button>
                <button onclick="switchTab('comparator')" id="btn-comparator" class="tab-inactive px-4 py-2 rounded-lg text-sm font-medium transition-all flex items-center gap-2">
                    <i class="fa-solid fa-chart-line"></i> Comparador
                </button>
            </div>
        </div>
    </nav>

    <div class="flex-1 overflow-hidden flex flex-col relative pb-6">
        
        <div id="view-dashboard" class="fade-in flex flex-col h-full gap-5">
            
            <div class="flex-none grid grid-cols-1 md:grid-cols-4 gap-4">
                <div class="card-dark p-5 flex flex-col justify-center h-24 border-l-4 border-l-emerald-500 relative overflow-hidden">
                    <p class="text-emerald-400 text-[10px] font-bold uppercase mb-1 tracking-wider">Monitorizadas</p>
                    <h3 class="text-3xl font-bold text-white z-10" id="kpi-companies">-</h3>
                    <div class="text-[10px] text-slate-500 mt-1 z-10">Empresas activas</div>
                    <i class="fa-solid fa-server absolute right-2 top-2 text-emerald-500/10 text-6xl"></i>
                </div>
                <div class="card-dark p-5 flex flex-col justify-center h-24 border-l-4 border-l-blue-500">
                    <p class="text-slate-400 text-[10px] font-bold uppercase mb-1 tracking-wider">Volumen Total</p>
                    <h3 class="text-2xl font-bold text-white" id="kpi-sales">- <span class="text-sm text-slate-500">GWh</span></h3>
                </div>
                <div class="card-dark p-5 flex flex-col justify-center h-24 border-l-4 border-l-purple-500">
                    <p class="text-slate-400 text-[10px] font-bold uppercase mb-1 tracking-wider">Estimación FNEE</p>
                    <h3 class="text-2xl font-bold text-blue-400" id="kpi-cost">- M€</h3>
                </div>
                <div class="card-dark p-5 flex flex-col justify-center h-24 border-l-4 border-l-amber-500">
                    <p class="text-slate-400 text-[10px] font-bold uppercase mb-1 tracking-wider">Líder Mercado</p>
                    <h3 class="text-lg font-bold text-white truncate" id="kpi-leader">-</h3>
                </div>
            </div>

            <div class="flex-1 grid grid-cols-1 lg:grid-cols-12 gap-6 min-h-0">
                
                <div class="lg:col-span-8 card-dark flex flex-col overflow-hidden">
                    <div class="flex-none p-3 border-b border-slate-700 bg-slate-900/50 flex justify-between items-center">
                        <h3 class="font-bold text-white text-sm flex items-center gap-2"><i class="fa-solid fa-list text-emerald-500"></i> Directorio de Empresas</h3>
                        <div class="relative">
                            <i class="fa-solid fa-search absolute left-3 top-2.5 text-slate-500 text-xs"></i>
                            <input type="text" id="search-input" placeholder="Buscar..." 
                                class="pl-8 pr-3 py-1.5 border border-slate-600 bg-slate-950 rounded text-xs w-64 text-white placeholder-slate-600 focus:border-emerald-500"
                                onkeyup="filterTable()">
                        </div>
                    </div>
                    
                    <div class="flex-1 overflow-y-auto bg-slate-900">
                        <table class="w-full text-sm text-left border-collapse table-fixed">
                            <thead>
                                <tr>
                                    <th style="width: 50%;">Empresa / Razón Social</th>
                                    <th style="width: 20%; text-align: right;">Último (GWh)</th>
                                    <th style="width: 15%; text-align: center;">Trend</th>
                                    <th style="width: 15%; text-align: center;">Ficha</th>
                                </tr>
                            </thead>
                            <tbody id="companies-table-body" class="text-slate-300 divide-y divide-slate-800">
                                </tbody>
                        </table>
                    </div>
                    <div class="flex-none p-2 bg-slate-950 border-t border-slate-800 text-center text-[10px] text-slate-500">
                        <span id="rows-count">0</span> empresas cargadas desde CSV
                    </div>
                </div>

                <div class="lg:col-span-4 card-dark flex flex-col overflow-hidden">
                    <div class="flex-none p-3 border-b border-slate-700 bg-slate-900/50">
                        <h3 class="font-bold text-white text-sm">Evolución Total (GWh)</h3>
                    </div>
                    <div class="flex-1 p-4 flex items-center justify-center relative">
                        <canvas id="marketChart"></canvas>
                    </div>
                </div>
            </div>
        </div>

        <div id="view-comparator" class="hidden fade-in flex flex-col h-full">
            <div class="flex h-full gap-6">
                <div class="w-1/4 flex flex-col gap-4">
                    <div class="card-dark p-5 flex-none">
                        <h3 class="font-bold text-white mb-4 text-xs uppercase tracking-wider text-emerald-400">1. Selección</h3>
                        <div class="relative mb-3">
                            <input type="text" id="comp-search" placeholder="Añadir competidor..." 
                                class="w-full px-3 py-2 border border-slate-700 bg-slate-950 rounded text-sm outline-none text-white focus:border-emerald-500"
                                onkeyup="searchCompetitor()">
                            <div id="comp-results" class="hidden absolute z-20 w-full mt-1 bg-slate-800 border border-slate-600 rounded shadow-xl max-h-40 overflow-y-auto text-slate-300 text-xs"></div>
                        </div>
                        <div id="selected-comps" class="space-y-2 max-h-60 overflow-y-auto"></div>
                        <p class="text-[10px] text-slate-500 italic text-center mt-3">Máximo 5 empresas</p>
                    </div>
                    
                    <div class="card-dark p-5 flex-none">
                        <h3 class="font-bold text-white mb-4 text-xs uppercase tracking-wider text-emerald-400">2. Métrica</h3>
                        <button onclick="setChartMode('absolute')" class="w-full text-left px-3 py-2 rounded mb-2 text-xs font-medium bg-emerald-500/10 text-emerald-400 border border-emerald-500/50 transition-all" id="mode-abs">
                            <i class="fa-solid fa-chart-bar mr-2"></i> Volumen Real
                        </button>
                        <button onclick="setChartMode('indexed')" class="w-full text-left px-3 py-2 rounded text-xs font-medium bg-slate-900 text-slate-400 border border-slate-700 hover:bg-slate-800 transition-all" id="mode-idx">
                            <i class="fa-solid fa-arrow-trend-up mr-2"></i> Crecimiento (%)
                        </button>
                    </div>
                </div>

                <div class="w-3/4 card-dark p-6 flex flex-col">
                    <h3 class="font-bold text-white mb-4 text-sm flex justify-between">
                        <span>Comparativa Estratégica</span>
                    </h3>
                    <div class="flex-1 relative w-full h-full">
                        <canvas id="comparatorChart"></canvas>
                    </div>
                </div>
            </div>
        </div>

    </div>

    <div id="modal" class="fixed inset-0 bg-slate-950/90 z-[100] hidden flex items-center justify-center p-4 backdrop-blur-sm">
        <div class="bg-slate-800 rounded-xl shadow-2xl w-full max-w-4xl border border-slate-700 animate-in fade-in zoom-in duration-200">
            <div class="bg-slate-900 p-4 flex justify-between items-center border-b border-slate-700">
                <div class="flex items-center gap-3">
                    <span class="bg-slate-700 text-[10px] px-2 py-0.5 rounded font-mono text-emerald-400 border border-slate-600" id="modal-cif">CIF</span>
                    <h2 class="text-lg font-bold text-white" id="modal-name">Empresa</h2>
                </div>
                <button onclick="closeModal()" class="text-slate-400 hover:text-white"><i class="fa-solid fa-times text-lg"></i></button>
            </div>
            <div class="p-6">
                <div class="grid grid-cols-4 gap-4 mb-6">
                    <div class="p-3 bg-slate-900 rounded border border-slate-700">
                        <p class="text-[10px] text-slate-500 uppercase font-bold">Último Año</p>
                        <p class="text-lg font-bold text-white mt-1" id="modal-sales-last">-</p>
                    </div>
                    <div class="p-3 bg-slate-900 rounded border border-slate-700">
                        <p class="text-[10px] text-slate-500 uppercase font-bold">Crecimiento (1y)</p>
                        <p class="text-lg font-bold text-white mt-1" id="modal-growth">-</p>
                    </div>
                    <div class="p-3 bg-slate-900 rounded border border-slate-700">
                        <p class="text-[10px] text-slate-500 uppercase font-bold">CAGR Total</p>
                        <p class="text-lg font-bold text-emerald-400 mt-1" id="modal-cagr">-</p>
                    </div>
                    <div class="p-3 bg-slate-900 rounded border border-slate-700">
                        <p class="text-[10px] text-slate-500 uppercase font-bold">Coste Est. FNEE</p>
                        <p class="text-lg font-bold text-blue-400 mt-1" id="modal-cost">-</p>
                    </div>
                </div>
                <div class="h-64 relative">
                    <canvas id="modalChart"></canvas>
                </div>
            </div>
        </div>
    </div>

</div>

<script>
    // Config ChartJS
    Chart.defaults.color = '#64748b';
    Chart.defaults.borderColor = '#334155';
    Chart.defaults.font.family = "'Segoe UI', sans-serif";

    // Variables Globales
    let DB = [];
    let YEARS = []; 
    let marketChartInstance = null;
    let comparatorChartInstance = null;
    let modalChartInstance = null;
    let selectedCompetitors = [];
    let comparatorMode = 'absolute';

    const fmtNum = (n) => new Intl.NumberFormat('es-ES', { maximumFractionDigits: 0 }).format(n);
    const fmtEur = (n) => new Intl.NumberFormat('es-ES', { style: 'currency', currency: 'EUR', maximumFractionDigits: 0 }).format(n);
    const calcCAGR = (start, end, periods) => (start > 0 && periods > 0) ? ((Math.pow(end / start, 1 / periods) - 1) * 100).toFixed(2) : 0;

    // --- CARGA CSV ---
    async function loadData() {
        try {
            const response = await fetch('ventas_boe.csv');
            if (!response.ok) throw new Error("No se encontró ventas_boe.csv");
            const text = await response.text();
            
            const lines = text.split('\n').map(l => l.trim()).filter(l => l);
            if(lines.length < 2) return;

            // Detectar cabecera y años
            const headers = lines[0].split(';');
            const yearMapping = [];
            headers.forEach((h, i) => {
                const match = h.match(/20\d{2}/);
                if (match) yearMapping.push({ index: i, year: parseInt(match[0]) });
            });

            if(yearMapping.length > 0) {
                YEARS = [...new Set(yearMapping.map(y => y.year))].sort((a,b) => a - b);
            } else {
                console.error("No se encontraron columnas de años (20xx)");
                return;
            }

            const newDB = [];
            for (let i = 1; i < lines.length; i++) {
                const cols = lines[i].split(';');
                if (cols.length < 2) continue;
                
                const cif = cols[0].trim().toUpperCase();
                const name = cols[1].trim();
                const sales = {};
                
                yearMapping.forEach(ym => {
                    if (cols[ym.index]) {
                        // Limpieza numérica robusta (1.000,00 o 1000.00)
                        let valStr = cols[ym.index].trim();
                        // Si tiene punto y coma, punto es mil, coma es decimal
                        if(valStr.indexOf(',') > -1 && valStr.indexOf('.') > -1) {
                             valStr = valStr.replace(/\./g, '').replace(',', '.');
                        } else if(valStr.indexOf(',') > -1) {
                             valStr = valStr.replace(',', '.');
                        }
                        sales[ym.year] = parseFloat(valStr) || 0;
                    } else {
                        sales[ym.year] = 0;
                    }
                });

                if (cif && name) newDB.push({ cif, name, sales });
            }
            
            DB = newDB;
            document.getElementById('rows-count').innerText = DB.length;
            
            // Auto-select Top 2 para comparador
            const lastY = YEARS[YEARS.length-1];
            const top2 = [...DB].sort((a,b) => (b.sales[lastY]||0) - (a.sales[lastY]||0)).slice(0,2);
            selectedCompetitors = top2.map(c => c.cif);
            
            renderDashboard();
            renderSelectedChips();

        } catch (err) {
            console.error("Error cargando CSV:", err);
        }
    }

    // --- INTERFAZ ---
    function switchTab(tabId) {
        ['dashboard', 'comparator'].forEach(t => {
            document.getElementById('view-' + t).classList.add('hidden');
            document.getElementById('btn-' + t).className = 'tab-inactive px-4 py-2 rounded-lg text-sm font-medium transition-all flex items-center gap-2 cursor-pointer';
        });
        document.getElementById('view-' + tabId).classList.remove('hidden');
        document.getElementById('btn-' + tabId).className = 'tab-active px-4 py-2 rounded-lg text-sm font-medium transition-all flex items-center gap-2 cursor-default';
        if(tabId === 'comparator') updateComparatorChart();
    }

    function renderDashboard() {
        if(DB.length === 0) return;
        
        const lastYear = YEARS[YEARS.length - 1];
        const prevYear = YEARS.length > 1 ? YEARS[YEARS.length - 2] : lastYear;

        let totalSalesLast = 0;
        let topGrower = { name: '-', growth: -Infinity };

        DB.forEach(c => {
            const sLast = c.sales[lastYear] || 0;
            const sPrev = c.sales[prevYear] || 0;
            totalSalesLast += sLast;
            const growth = sLast - sPrev;
            if (growth > topGrower.growth) topGrower = { name: c.name, growth: growth };
        });

        document.getElementById('kpi-companies').innerText = DB.length;
        document.getElementById('kpi-sales').innerHTML = fmtNum(totalSalesLast) + ' <span class="text-sm text-slate-500">GWh</span>';
        document.getElementById('kpi-cost').innerText = fmtEur(totalSalesLast * 239000).slice(0,-2) + ' M€';
        document.getElementById('kpi-leader').innerText = topGrower.name;
        
        renderTable(DB);
        renderMarketChart();
    }

    function renderTable(data) {
        const lastYear = YEARS[YEARS.length - 1];
        const prevYear = YEARS.length > 1 ? YEARS[YEARS.length - 2] : lastYear;
        const tbody = document.getElementById('companies-table-body');
        tbody.innerHTML = '';
        
        const sorted = [...data].sort((a,b) => (b.sales[lastYear]||0) - (a.sales[lastYear]||0)).slice(0, 100);

        sorted.forEach(c => {
            const sLast = c.sales[lastYear] || 0;
            const sPrev = c.sales[prevYear] || 0;
            const trend = sPrev > 0 ? ((sLast - sPrev)/sPrev * 100).toFixed(1) : 0;
            const trendClass = trend >= 0 ? 'bg-emerald-500/10 text-emerald-400' : 'bg-rose-500/10 text-rose-400';
            const icon = trend >= 0 ? 'fa-caret-up' : 'fa-caret-down';
            
            const row = `
                <tr class="border-b border-slate-800 transition-colors">
                    <td class="truncate" title="${c.name}">
                        <div class="font-bold text-white truncate">${c.name}</div>
                        <div class="text-[10px] text-slate-500 font-mono">${c.cif}</div>
                    </td>
                    <td class="text-right font-mono text-slate-300 font-bold">${fmtNum(sLast)}</td>
                    <td class="text-center">
                        <span class="text-xs font-bold ${trendClass} px-2 py-1 rounded">
                           <i class="fa-solid ${icon} mr-1"></i>${Math.abs(trend)}%
                        </span>
                    </td>
                    <td class="text-center">
                        <button onclick="openModal('${c.cif}')" class="text-slate-400 hover:text-blue-400 hover:bg-blue-500/10 p-2 rounded transition">
                            <i class="fa-solid fa-eye"></i>
                        </button>
                    </td>
                </tr>
            `;
            tbody.innerHTML += row;
        });
    }

    function renderMarketChart() {
        const ctx = document.getElementById('marketChart').getContext('2d');
        const totals = YEARS.map(y => DB.reduce((sum, c) => sum + (c.sales[y]||0), 0));

        if(marketChartInstance) marketChartInstance.destroy();

        marketChartInstance = new Chart(ctx, {
            type: 'bar',
            data: {
                labels: YEARS,
                datasets: [{
                    label: 'Volumen (GWh)',
                    data: totals,
                    backgroundColor: '#2dd4bf',
                    borderRadius: 4,
                    barThickness: 40
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                plugins: { legend: { display: false } },
                scales: { y: { beginAtZero: true, grid: { color: '#1e293b' } }, x: { grid: { display: false } } }
            }
        });
    }

    function filterTable() {
        const term = document.getElementById('search-input').value.toLowerCase();
        const filtered = DB.filter(c => c.name.toLowerCase().includes(term) || c.cif.toLowerCase().includes(term));
        renderTable(filtered);
    }

    // --- COMPARADOR ---
    function searchCompetitor() {
        const term = document.getElementById('comp-search').value.toLowerCase();
        const resultsDiv = document.getElementById('comp-results');
        resultsDiv.innerHTML = '';
        if(term.length < 2) { resultsDiv.classList.add('hidden'); return; }

        const matches = DB.filter(c => c.name.toLowerCase().includes(term) && !selectedCompetitors.includes(c.cif)).slice(0, 10);
        if(matches.length > 0) {
            resultsDiv.classList.remove('hidden');
            matches.forEach(c => {
                const div = document.createElement('div');
                div.className = 'p-2 hover:bg-slate-700 cursor-pointer border-b border-slate-700 text-slate-300';
                div.innerHTML = `<div class="font-bold text-white">${c.name}</div><div class="text-[10px] text-slate-500">${c.cif}</div>`;
                div.onclick = () => addCompetitor(c.cif);
                resultsDiv.appendChild(div);
            });
        } else { resultsDiv.classList.add('hidden'); }
    }

    function addCompetitor(cif) {
        if(selectedCompetitors.length >= 5) { alert("Máximo 5"); return; }
        selectedCompetitors.push(cif);
        document.getElementById('comp-search').value = '';
        document.getElementById('comp-results').classList.add('hidden');
        renderSelectedChips();
        updateComparatorChart();
    }

    function removeCompetitor(cif) {
        selectedCompetitors = selectedCompetitors.filter(c => c !== cif);
        renderSelectedChips();
        updateComparatorChart();
    }

    function renderSelectedChips() {
        const container = document.getElementById('selected-comps');
        container.innerHTML = '';
        const colors = ['#3b82f6', '#ec4899', '#22c55e', '#f97316', '#8b5cf6'];
        
        selectedCompetitors.forEach((cif, idx) => {
            const comp = DB.find(c => c.cif === cif);
            const div = document.createElement('div');
            div.className = 'flex items-center justify-between p-2 bg-slate-950 rounded border border-slate-700 text-xs text-white';
            div.innerHTML = `<div class="flex items-center gap-2 overflow-hidden"><div class="w-2 h-2 rounded-full flex-shrink-0" style="background:${colors[idx]}"></div><span class="truncate">${comp.name}</span></div><button onclick="removeCompetitor('${cif}')" class="text-slate-500 hover:text-rose-500"><i class="fa-solid fa-times"></i></button>`;
            container.appendChild(div);
        });
    }

    function setChartMode(mode) {
        comparatorMode = mode;
        const btnAbs = document.getElementById('mode-abs');
        const btnIdx = document.getElementById('mode-idx');
        const activeClass = "w-full text-left px-3 py-2 rounded mb-2 text-xs font-medium bg-emerald-500/10 text-emerald-400 border border-emerald-500/50 transition-all";
        const inactiveClass = "w-full text-left px-3 py-2 rounded mb-2 text-xs font-medium bg-slate-900 text-slate-400 border border-slate-700 hover:bg-slate-800 transition-all";
        
        if(mode === 'absolute') { btnAbs.className = activeClass; btnIdx.className = inactiveClass; }
        else { btnAbs.className = inactiveClass; btnIdx.className = activeClass; }
        updateComparatorChart();
    }

    function updateComparatorChart() {
        const ctx = document.getElementById('comparatorChart').getContext('2d');
        const colors = ['#3b82f6', '#ec4899', '#22c55e', '#f97316', '#8b5cf6'];
        
        const datasets = selectedCompetitors.map((cif, idx) => {
            const comp = DB.find(c => c.cif === cif);
            let data = YEARS.map(y => comp.sales[y] || 0);
            if (comparatorMode === 'indexed') {
                const base = data[0] || 1; 
                data = data.map(v => (v/base)*100);
            }
            return { label: comp.name, data: data, borderColor: colors[idx], backgroundColor: colors[idx], borderWidth: 2, tension: 0.3, pointRadius: 3 };
        });

        if(comparatorChartInstance) comparatorChartInstance.destroy();

        comparatorChartInstance = new Chart(ctx, {
            type: 'line',
            data: { labels: YEARS, datasets: datasets },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                interaction: { mode: 'index', intersect: false },
                scales: { y: { beginAtZero: false, grid: { color: '#1e293b' }, ticks: { color: '#64748b' } }, x: { grid: { display: false }, ticks: { color: '#94a3b8' } } },
                plugins: { legend: { display: false } }
            }
        });
    }

    // --- MODAL ---
    function openModal(cif) {
        const comp = DB.find(c => c.cif === cif);
        if(!comp) return;
        const lastY = YEARS[YEARS.length-1];
        const sLast = comp.sales[lastY] || 0;
        const sPrev = comp.sales[YEARS[0]] || 0;
        const growth = sPrev > 0 ? ((sLast - sPrev)/sPrev * 100).toFixed(1) + '%' : '-';
        const cagr = calcCAGR(sPrev, sLast, YEARS.length-1) + '%';
        
        document.getElementById('modal').classList.remove('hidden');
        document.getElementById('modal-cif').innerText = comp.cif;
        document.getElementById('modal-name').innerText = comp.name;
        document.getElementById('modal-sales-last').innerText = fmtNum(sLast);
        document.getElementById('modal-growth').innerText = growth;
        document.getElementById('modal-cagr').innerText = cagr;
        document.getElementById('modal-cost').innerText = fmtEur(sLast * 239000); 

        const ctx = document.getElementById('modalChart').getContext('2d');
        if(modalChartInstance) modalChartInstance.destroy();
        modalChartInstance = new Chart(ctx, {
            type: 'line',
            data: {
                labels: YEARS,
                datasets: [{ label: 'Ventas', data: YEARS.map(y => comp.sales[y]||0), borderColor: '#2563eb', backgroundColor: 'rgba(37, 99, 235, 0.1)', fill: true }]
            },
            options: { responsive: true, maintainAspectRatio: false, scales: { y: { beginAtZero: true, grid: {color:'#1e293b'} } } }
        });
    }
    
    function closeModal() { document.getElementById('modal').classList.add('hidden'); }

    // Init
    loadData();
</script>
```